import React from 'react';
import './Spinner.css'

const Spinner = () => {
    return (
        <div className='spin-wrapper'>
            <div className="spinner">
                </div>
        </div>
    );
};

export default Spinner;
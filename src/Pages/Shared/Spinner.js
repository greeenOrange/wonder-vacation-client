import React from 'react';
import './Spinner.css'

const Spinner = () => {
    return (
        <div className='spin-wrapper'>
            <div class="spinner">
                </div>
        </div>
    );
};

export default Spinner;